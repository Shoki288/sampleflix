apply from: "$rootProject.projectDir/gradle/repository_template.gradle"
android {
    namespace 'com.example.search_repository'
}

dependencies {
    api project(":repository_favorite")

    /** paging **/
    implementation "androidx.paging:paging-runtime:$paging_version"
}