apply from: "$rootProject.projectDir/gradle/feature_template.gradle"
android {
    namespace 'com.example.feature_search'
}

dependencies {
    api project(":repository_category")

    implementation "androidx.navigation:navigation-fragment-ktx:$nav_version"
}